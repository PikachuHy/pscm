# Guile 1.8 Feature Audit for pscm

**Date**: 2026-05-02
**Status**: in-progress

## Special Forms

| Special Form | Status | Implementation | Notes |
|-------------|--------|---------------|-------|
| `define` | present | `src/c/define.cc:eval_define` | Handles `(define var expr)`, `(define (name args...) body...)`, and curried define forms. Variable bindings respect lexical scoping (nested/lambda envs vs top-level/module). |
| `lambda` | present | `src/c/eval.cc:eval_lambda` (line 123) | Supports normal parameter lists, dotted rest params `(lambda (x . y) ...)`, and single-symbol rest params `(lambda x ...)`. |
| `if` | present | `src/c/eval.cc:eval_if` (line 167) | Standard conditional with consequent and optional alternative. Returns unspecified value (none) when no else branch. |
| `cond` | present | `src/c/cond.cc:eval_cond` | Supports `else` clause, `=>` arrow syntax (uses `is_literal` heuristic similar to Guile's `literal_p`), and normal expression sequences. |
| `case` | present | `src/c/case.cc:eval_case` | Supports `else` clause and multiple datum comparisons via `eqv?`. Returns `#f` when no clause matches. |
| `do` | present | `src/c/do.cc:eval_do` | Full implementation with parallel binding semantics (all step expressions evaluated before any variables are updated). Supports optional step expressions and body expressions. |
| `let` | present | `src/c/let.cc:expand_let` | Implemented as source-to-source transformation to `((lambda ...) ...)`. Supports named let with full correct semantics (args evaluated in outer env before letrec binding). |
| `let*` | present | `src/c/let.cc:expand_letstar` | Implemented as source-to-source transformation to nested `let`. |
| `letrec` | present | `src/c/let.cc:expand_letrec` | Implemented as source-to-source transformation to `(let ((var #f)) (set! var ...) ...)`. |
| `and` | present | `src/c/eval.cc:eval_and` (line 183) | Short-circuit evaluation; returns `#t` with no arguments, the last expression's value otherwise, or `#f` on first falsy. |
| `or` | present | `src/c/eval.cc:eval_or` (line 208) | Short-circuit evaluation; returns `#f` with no arguments, the first truthy value, or the last expression's value. |
| `delay` | present | `src/c/delay.cc:eval_delay` | Creates a promise (zero-arg thunk). `force` (`scm_c_force`) implements R5RS-compliant re-entrant semantics: if a promise is forced while already being forced, the inner force caches the value. |
| `quasiquote` | present | `src/c/quasiquote.cc:eval_quasiquote` | Implements `unquote`, `unquote-splicing`, nested quasiquote, and vector quasiquote (`#( ... )`). Uses direct evaluation approach (similar to Guile 1.8's `iqq`). |
| `quote` | present | `src/c/eval.cc:eval_quote` (line 65) | Simple: returns the first argument unevaluated. |
| `set!` | present | `src/c/eval.cc:eval_set` (line 71) | Searches local environment first (no parent search), then parent environments, then current module. Creates new binding if not found anywhere. |
| `begin` | present | `src/c/eval.cc` inline (line 424) | Evaluates all sub-expressions in sequence, returns the last value. Returns `#f` when no sub-expressions (unspecified in R5RS). |
| `define-macro` | present | `src/c/macro.cc:eval_define_macro` | Explicit renaming macros. Supports both `(define-macro (name args...) body...)` and `(define-macro name transformer)` forms. Handles rest parameters in macro args. |
| `define-module` | partial | `src/c/module.cc:eval_define_module` | Creates modules and registers them. Supports options: `#:use-module`, `#:export`, `#:pure`, `#:autoload`, `#:no-backtrace`. Option parsing noted as "simplified version" (comment on line 726). |
| `use-modules` | present | `src/c/module.cc:eval_use_modules` | Resolves module names and adds their public interfaces to the current module's uses list. |
| `export` | present | `src/c/module.cc:eval_export` | Marks symbols for export from the current module. |
| `re-export` | partial | `src/c/module.cc:eval_re_export` | Re-exports symbols from other modules. Very lenient -- allows re-exporting symbols not found in the module (comment on line 908: "allows re-exporting symbols that will be available later"). A stricter implementation would error on missing symbols. |
| `define-public` | present | `src/c/module.cc:eval_define_public` | Combines `define` with `export`. |
| `call/cc` / `call-with-current-continuation` | present | `src/c/eval.cc:eval_call_cc` (line 233) | Implemented via `setjmp`/`longjmp` in `continuation.cc`. Handled as both a special form (symbol dispatch) and a function value (C++ function dispatch at line 593). Stack data is copied on capture, restored on invocation. |
| `apply` | present | `src/c/apply.cc:eval_apply` | Handles procedure, function, and continuation application. Supports the standard `(apply proc arg1 ... args-list)` syntax. Contains special-case handling for nested `apply`/`map`/`call/cc` application (lines 331-392). |
| `map` | present | `src/c/map.cc:eval_map` | Works with procedures and functions. Supports multiple list arguments (shortest list determines length). Note: heap-allocates `list_ptrs` array (line 57), which may interact poorly with continuations. |
| `for-each` | partial | `src/c/for_each.cc:eval_for_each` | Hard-coded `MAX_FOR_EACH_ARGS = 10` limit (line 59). Otherwise complete: supports procedures and functions, multiple lists, proper length validation. |
| `map-in-order` | present | `src/c/map.cc:eval_map` | Alias for `map` (line 456 in eval.cc dispatches `map-in-order` to `eval_map`). In pscm, `map` already preserves order. |
| `call-with-values` | present | `src/c/values.cc:eval_call_with_values` | Calls producer, passes values list to consumer. `values` implemented as a C function (`scm_c_values`) that returns its arguments as a list. |
| `dynamic-wind` | present | `src/c/dynwind.cc:eval_dynamic_wind` | Full implementation with wind chain management. Supports in-guard/thunk/out-guard calling, wind chain copy/compare for continuations, and proper unwind/rewind semantics. |

## Built-in Procedures

All C-level built-in procedures are registered via `scm_define_function()` or `scm_define_vararg_function()` in their respective `init_*()` functions. The tables below compare pscm's current coverage against the Guile 1.8 reference.

### List Procedures

Registered in `src/c/list.cc:init_list()`.

| Procedure | Status | Notes |
|-----------|--------|-------|
| `cons` | present | |
| `car` | present | |
| `cdr` | present | |
| `caar` | missing | Accessible as `(car (car x))` |
| `cadr` | present | |
| `cdar` | missing | Accessible as `(cdr (car x))` |
| `cddr` | present | |
| `caaar` | missing | Accessible as `(car (car (car x)))` |
| `caadr` | missing | Accessible as `(car (car (cdr x)))` |
| `cadar` | missing | Accessible as `(car (cdr (car x)))` |
| `caddr` | present | |
| `cdaar` | missing | Accessible as `(cdr (car (car x)))` |
| `cdadr` | missing | Accessible as `(cdr (car (cdr x)))` |
| `cddar` | missing | Accessible as `(cdr (cdr (car x)))` |
| `cdddr` | missing | Accessible as `(cdr (cdr (cdr x)))` |
| `null?` | present | Registered in `src/c/predicate.cc` |
| `list?` | present | Registered in `src/c/predicate.cc` |
| `list` | present | |
| `length` | present | |
| `append` | present | |
| `reverse` | present | |
| `list-tail` | present | |
| `list-ref` | present | |
| `list-head` | present | |
| `last-pair` | present | |
| `memq` | present | |
| `memv` | present | |
| `member` | present | |
| `set-car!` | present | |
| `set-cdr!` | present | |

### Association List Procedures

Registered in `src/c/alist.cc:init_alist()`.

| Procedure | Status | Notes |
|-----------|--------|-------|
| `assq` | present | |
| `assv` | present | |
| `assoc` | present | |
| `assq-ref` | missing | `assoc-ref` exists but not `assq-ref` |
| `assq-set!` | present | |
| `assoc-ref` | present | |
| `assoc-set!` | present | |
| `assoc-remove!` | present | |
| `acons` | present | |

### String Procedures

Registered in `src/c/string.cc:init_string()`.

| Procedure | Status | Notes |
|-----------|--------|-------|
| `string?` | present | Registered in `src/c/predicate.cc` |
| `make-string` | present | |
| `string` | present | |
| `string-length` | present | |
| `string-ref` | present | |
| `string-set!` | present | |
| `string=?` | present | |
| `string<?` | present | |
| `string<=?` | present | |
| `string>?` | present | |
| `string>=?` | present | |
| `string-ci=?` | present | |
| `string-ci<?` | present | |
| `string-ci<=?` | present | |
| `string-ci>?` | present | |
| `string-ci>=?` | present | |
| `substring` | present | |
| `string-append` | present | |
| `string->list` | present | |
| `list->string` | present | |
| `string-fill!` | missing | No implementation found |
| `string->number` | present | Also registered in `init_string` |
| `number->string` | present | Also registered in `init_string` |

### Vector Procedures

Registered in `src/c/vector.cc:init_vector()`.

| Procedure | Status | Notes |
|-----------|--------|-------|
| `vector?` | present | Registered in `src/c/predicate.cc` |
| `make-vector` | present | |
| `vector` | present | |
| `vector-length` | present | |
| `vector-ref` | present | |
| `vector-set!` | present | |
| `vector->list` | present | |
| `list->vector` | present | |
| `vector-fill!` | missing | No implementation found |

### Hash Table Procedures

All 15 hash table procedures are present. Registered in `src/c/hash_table.cc:init_hash_table()`.

| Procedure | Status | Notes |
|-----------|--------|-------|
| `make-hash-table` | present | |
| `hash-ref` | present | Uses `equal?` comparison |
| `hash-set!` | present | Uses `equal?` comparison |
| `hash-remove!` | present | Uses `equal?` comparison |
| `hashq-ref` | present | Uses `eq?` comparison |
| `hashq-set!` | present | Uses `eq?` comparison |
| `hashq-remove!` | present | Uses `eq?` comparison |
| `hashv-ref` | present | Uses `eqv?` comparison |
| `hashv-set!` | present | Uses `eqv?` comparison |
| `hashv-remove!` | present | Uses `eqv?` comparison |
| `hash-get-handle` | present | Uses `equal?` comparison |
| `hashq-get-handle` | present | Uses `eq?` comparison |
| `hashv-get-handle` | present | Uses `eqv?` comparison |
| `hash-create-handle!` | present | Uses `equal?` comparison |
| `hashq-create-handle!` | present | Uses `eq?` comparison |
| `hashv-create-handle!` | present | Uses `eqv?` comparison |
| `hash-fold` | present | Uses `equal?` comparison internally |

### Numeric Procedures

Registered in `src/c/number.cc:init_number()` and `src/c/predicate.cc:init_predicate()`.

| Procedure | Status | Notes |
|-----------|--------|-------|
| `number?` | present | |
| `complex?` | present | All numbers return true (no complex type yet) |
| `real?` | present | All numbers return true (no complex type yet) |
| `rational?` | present | True for integers and ratios only (not floats) |
| `integer?` | present | True for fixnums only |
| `exact?` | present | True for fixnums and ratios |
| `inexact?` | present | True for floats only |
| `=` | present | Chained comparison supported |
| `<` | present | Chained comparison supported |
| `>` | present | Chained comparison supported |
| `<=` | present | Chained comparison supported |
| `>=` | present | Chained comparison supported |
| `zero?` | present | |
| `positive?` | present | |
| `negative?` | present | |
| `odd?` | present | |
| `even?` | present | |
| `max` | present | Supports integer and float |
| `min` | present | Supports integer and float |
| `+` | present | Generic, neutral element 0 |
| `-` | present | Handles both unary (negate) and binary forms |
| `*` | present | Generic, neutral element 1 |
| `/` | present | Handles unary, binary, and multi-arg forms; returns ratios when exact |
| `abs` | present | Integer only |
| `quotient` | present | |
| `remainder` | present | |
| `modulo` | present | |
| `sqrt` | present | Float result only |
| `expt` | present | Integer base and exponent only; limited negative exponent support |
| `gcd` | present | |
| `lcm` | present | |

### Character Procedures

Registered in `src/c/char.cc:init_char()`.

All 20 character procedures from the Guile 1.8 reference are present.

| Procedure | Status | Notes |
|-----------|--------|-------|
| `char?` | present | |
| `char=?` | present | Binary only |
| `char<?` | present | Binary only |
| `char>?` | present | Binary only |
| `char<=?` | present | Binary only |
| `char>=?` | present | Binary only |
| `char-ci=?` | present | Binary only |
| `char-ci<?` | present | Binary only |
| `char-ci>?` | present | Binary only |
| `char-ci<=?` | present | Binary only |
| `char-ci>=?` | present | Binary only |
| `char-alphabetic?` | present | |
| `char-numeric?` | present | |
| `char-whitespace?` | present | |
| `char-upper-case?` | present | |
| `char-lower-case?` | present | |
| `char->integer` | present | |
| `integer->char` | present | Range restricted to 0-255 |
| `char-upcase` | present | |
| `char-downcase` | present | |

### Port/IO Procedures

Registered in `src/c/port.cc:init_port()` and `src/c/string.cc:init_string()`.

| Procedure | Status | Notes |
|-----------|--------|-------|
| `input-port?` | present | |
| `output-port?` | present | |
| `current-input-port` | present | Lazy-initialized wrapping stdin |
| `current-output-port` | present | Lazy-initialized wrapping stdout |
| `current-error-port` | present | Lazy-initialized wrapping stderr |
| `set-current-input-port` | present | |
| `set-current-output-port` | present | |
| `set-current-error-port` | present | |
| `open-input-file` | present | |
| `open-output-file` | present | |
| `close-input-port` | present | |
| `close-output-port` | present | |
| `open-input-string` | present | |
| `open-output-string` | present | |
| `get-output-string` | present | |
| `read` | present | From any input port |
| `write` | present | Registered in `init_string` |
| `display` | present | Registered in `init_string` |
| `newline` | present | Registered in `init_string` |
| `read-char` | present | Registered in `init_port` |
| `peek-char` | present | |
| `write-char` | present | |
| `eof-object?` | present | |
| `char-ready?` | present | |
| `force-output` | present | Extra, not in basic R5RS but in Guile 1.8 |
| `call-with-input-file` | present | Extra convenience |
| `call-with-output-file` | present | Extra convenience |
| `call-with-input-string` | present | Extra convenience |
| `call-with-output-string` | present | Extra convenience |
| `make-soft-port` | present | Guile 1.8 extension |

### Symbol Procedures

Registered in `src/c/symbol.cc:init_symbol()`. Predicate `symbol?` is in `src/c/predicate.cc`.

| Procedure | Status | Notes |
|-----------|--------|-------|
| `symbol?` | present | |
| `symbol->string` | present | |
| `string->symbol` | present | |
| `gensym` | present | |

### Predicate Procedures

Registered in `src/c/predicate.cc:init_predicate()`.

| Procedure | Status | Notes |
|-----------|--------|-------|
| `boolean?` | present | |
| `pair?` | present | |
| `symbol?` | present | |
| `number?` | present | |
| `string?` | present | |
| `char?` | present | Registered in `init_char` |
| `vector?` | present | |
| `procedure?` | present | |
| `null?` | present | |
| `eof-object?` | present | Registered in `init_port` |
| `list?` | present | |
| `not` | present | |
| `noop` | present | |

### Equality Procedures

Registered in `src/c/eq.cc:init_eq()`.

| Procedure | Status | Notes |
|-----------|--------|-------|
| `eq?` | present | Pointer equality with value-based fallback for immutables |
| `eqv?` | present | Value equality for numbers/chars, pointer equality for compound types |
| `equal?` | present | Recursive structural equality |

### Control Procedures

| Procedure | Status | Notes |
|-----------|--------|-------|
| `apply` | present | Registered in `src/c/apply.cc` as vararg, also a special form |
| `map` | present | Registered in `src/c/map.cc` as vararg, also a special form |
| `map-in-order` | present | Alias for `map` |
| `for-each` | partial | Implemented as special form, not as registered built-in function. `src/c/for_each.cc:eval_for_each` with MAX_FOR_EACH_ARGS=10 limit |
| `force` | present | Registered in `src/c/delay.cc` |
| `call-with-current-continuation` | present | Registered in `src/c/continuation.cc` |
| `call/cc` | present | Alias for `call-with-current-continuation` |
| `values` | present | Registered in `src/c/values.cc` |
| `call-with-values` | present | Implemented as special form |
| `dynamic-wind` | present | Implemented as special form |

### Module Procedures

Registered in `src/c/module.cc:init_modules()`. The Guile 1.8 module API also includes getter procedures and convenience functions beyond what pscm currently exposes.

| Procedure | Status | Notes |
|-----------|--------|-------|
| `module?` | present | |
| `current-module` | present | |
| `set-current-module` | present | |
| `resolve-module` | present | |
| `module-ref` | present | Supports optional default value and `%module-public-interface` special handling |
| `module-bound?` | present | |
| `module-map` | present | Iterates over obarray bindings |
| `module-use!` | present | Adds module to uses list |
| `set-module-binder!` | present | |
| `module-export!` | missing | Use `export` special form instead |
| `module-name` | missing | `module->name` C field exists but not exposed as Scheme procedure |
| `module-public-interface` | partial | Accessible via `(module-ref mod '%module-public-interface)`; no direct `module-public-interface` procedure |
| `module-kind` | missing | `module->kind` C field exists but not exposed |
| `module-obarray` | missing | Not exposed to Scheme |
| `module-binder` | partial | Setter (`set-module-binder!`) exists but getter (`module-binder`) does not |
| `resolve-interface` | missing | `scm_ensure_public_interface()` exists as internal C helper, not exposed to Scheme |
| `make-module` | missing | Use `define-module` special form or `scm_make_module` C API |
| `save-module` | missing | Not implemented (used by Guile for compilation) |

### Variable Procedures

Registered in `src/c/variable.cc:init_variable()`.

| Procedure | Status | Notes |
|-----------|--------|-------|
| `make-variable` | present | |
| `make-undefined-variable` | present | |
| `variable?` | present | |
| `variable-ref` | present | |
| `variable-set!` | present | |
| `variable-bound?` | present | |

### Load/Eval Procedures

Registered in `src/c/eval.cc:init_eval()` and `src/c/load.cc:init_load()`.

| Procedure | Status | Notes |
|-----------|--------|-------|
| `eval` | present | |
| `defined?` | present | |
| `load` | present | Delegates to `primitive-load` |
| `primitive-load` | present | Core file loading implementation |

### Macro Procedures

Registered in `src/c/macro.cc:init_macro()`.

| Procedure | Status | Notes |
|-----------|--------|-------|
| `macroexpand` | present | Recursively expands all macros |
| `macroexpand-1` | present | Single expansion step |

### Smob Procedures

Registered in `src/c/smob.cc:init_smob()`.

| Procedure | Status | Notes |
|-----------|--------|-------|
| `scm-make-smob-type` | present | |
| `scm-make-smob` | present | |
| `scm-smob?` | present | |
| `scm-smob-data` | present | |

### Catch/Throw Procedures

Registered in `src/c/throw.cc:init_throw()`.

| Procedure | Status | Notes |
|-----------|--------|-------|
| `catch` | present | |
| `throw` | present | |

### Debug/Read Options

Macro-defined procedures in `src/c/debug.cc` and `src/c/read.cc`. The underlying C functions `debug-options-interface` and `read-options-interface` are registered as vararg functions. The user-facing `debug-set!`, `debug-enable`, `debug-disable`, `read-set!`, `read-enable`, and `read-disable` are defined as Scheme macros.

| Procedure | Status | Notes |
|-----------|--------|-------|
| `debug-options-interface` | present | |
| `read-options-interface` | present | |
| `debug-set!` | present | Macro form |
| `debug-enable` | present | Macro form |
| `debug-disable` | present | Macro form |
| `read-set!` | present | Macro form |
| `read-enable` | present | Macro form |
| `read-disable` | present | Macro form |

### Other Procedures

| Procedure | Status | Notes |
|-----------|--------|-------|
| `gc` | present | Registered in `src/c/init.cc` |
| `exit` | present | Registered in `src/c/exit.cc` |
| `procedure-name` | present | Registered in `src/c/procedure.cc` |
| `procedure-property` | missing | Not implemented |
| `set-procedure-property!` | missing | Not implemented |
| `error` | missing | No `error` built-in. Errors use `throw` with `"error"` key internally |
| `module-export!` | missing | Use `export` special form instead |

### Summary

| Category | Present | Partial | Missing | Total |
|----------|---------|---------|---------|-------|
| List | 18 | 0 | 9 | 27 |
| Association List | 6 | 0 | 1 | 7 |
| String | 20 | 0 | 1 | 21 |
| Vector | 7 | 0 | 1 | 8 |
| Hash Table | 16 | 0 | 0 | 16 |
| Numeric | 30 | 0 | 0 | 30 |
| Character | 20 | 0 | 0 | 20 |
| Port/IO | 31 | 0 | 0 | 31 |
| Symbol | 4 | 0 | 0 | 4 |
| Predicates | 12 | 0 | 0 | 12 |
| Equality | 3 | 0 | 0 | 3 |
| Control | 9 | 1 | 0 | 10 |
| Module | 9 | 2 | 7 | 18 |
| Variable | 6 | 0 | 0 | 6 |
| Load/Eval | 4 | 0 | 0 | 4 |
| Macro | 2 | 0 | 0 | 2 |
| Smob | 4 | 0 | 0 | 4 |
| Catch/Throw | 2 | 0 | 0 | 2 |
| Debug/Read Options | 7 | 0 | 0 | 7 |
| Other | 2 | 0 | 3 | 5 |

**Overall: 206 present, 3 partial, 22 missing, 230 total in Guile 1.8 reference.**

## Module System

Source: `src/c/module.cc` (~1212 lines), `src/c/pscm_types.h` (SCM_Module struct, lines 124-135), `src/c/eval.cc` (special form dispatch, lines 333-351).

### C-Level Module Functions in module.cc

All module operations implemented at the C level, grouped by purpose.

#### Creation and Resolution

| Function | Line | Status | Description |
|----------|------|--------|-------------|
| `scm_make_module(SCM_List *name, int obarray_size)` | 16 | internal | Creates a new SCM_Module with obarray, null fields |
| `scm_resolve_module(SCM_List *name)` | 297 | internal | Resolve module by name: check registry, load from filesystem via %load-path |
| `scm_c_resolve_module(SCM *name_arg)` | 554 | public C API | C wrapper: takes SCM list, delegates to scm_resolve_module |
| `scm_c_resolve_module(const char *name)` | 566 | public C API | C overload: parses C string, e.g. scm_c_resolve_module("(ice-9 common-list)") |
| `scm_c_define_module(const char *name, void (*init)(void*), void *data)` | 1109 | public C API | Resolve module, run init function with module as current |

#### Variable Lookup

| Function | Line | Status | Description |
|----------|------|--------|-------------|
| `module_obarray_lookup(SCM_Module *, SCM_Symbol *)` | 69 | internal helper | Direct obarray hash lookup; returns value or nullptr |
| `module_search_variable(SCM_Module *, SCM_Symbol *)` | 93 | internal helper | Full search: obarray -> uses list -> root module -> binder -> autoload |
| `module_find_variable_module(SCM_Module *, SCM_Symbol *)` | 195 | internal helper | Find which module contains a variable (for updates) |
| `scm_module_variable(SCM_Module *, SCM_Symbol *, bool definep)` | 226 | internal | Wrapper for module_search_variable, returns #f on miss |
| `scm_module_lookup(SCM *, SCM *)` | 601 | Scheme-level | Lookup symbol in module, returns variable object or error |
| `scm_c_module_lookup(SCM *, const char *)` | 575 | public C API | C string variant of module-lookup |
| `scm_c_module_ref(SCM_List *args)` | 635 | Scheme-level | Vararg: (module-ref module symbol [default]). Handles `%module-public-interface` |

#### Definition

| Function | Line | Status | Description |
|----------|------|--------|-------------|
| `scm_c_define(const char *name, SCM *val)` | 511 | public C API | Define in current module, returns variable |
| `scm_c_define_gsubr(const char *name, int req, int opt, int rst, SCM *(*fcn)(void))` | 525 | public C API | Register C function in current module |
| `scm_module_define(SCM *, SCM *, SCM *)` | 620 | Scheme-level | Define in specified module |
| `scm_c_module_define(SCM *, const char *, SCM *)` | 590 | public C API | C string variant of module-define |

#### Export and Public Interface

| Function | Line | Status | Description |
|----------|------|--------|-------------|
| `scm_module_export(SCM_Module *, SCM_Symbol *)` | 474 | internal | Add symbol to exports list; sync to public interface |
| `scm_ensure_public_interface(SCM_Module *)` | 445 | internal | Create/get public interface module; populate from exports |
| `scm_update_module_public_interface(SCM_Module *)` | 492 | internal | Force rebuild of public interface |

#### Use/Import

| Function | Line | Status | Description |
|----------|------|--------|-------------|
| `scm_c_module_use(SCM_List *args)` | 1035 | Scheme-level | (module-use! module spec): add module to uses list |
| `scm_c_use_module(const char *name)` | 1148 | public C API | Import module into current module from C |

#### Binder

| Function | Line | Status | Description |
|----------|------|--------|-------------|
| `scm_c_set_module_binder(SCM *, SCM *)` | 1135 | Scheme-level | (set-module-binder! module proc) |

#### Current Module Management

| Function | Line | Status | Description |
|----------|------|--------|-------------|
| `scm_current_module()` | 42 | internal | Get current module (lazy-init root if none) |
| `scm_set_current_module(SCM *)` | 57 | internal | Set current module; returns old module |
| `scm_c_current_module(SCM *)` | 499 | Scheme-level | current-module C wrapper |
| `scm_c_set_current_module(SCM *)` | 504 | Scheme-level | set-current-module C wrapper |
| `scm_c_call_with_current_module(SCM *, SCM *(*)(void*), void*)` | 1094 | public C API | Temporarily switch module, call func, restore |

#### Introspection and Predicates

| Function | Line | Status | Description |
|----------|------|--------|-------------|
| `scm_c_module_map(SCM_List *args)` | 950 | Scheme-level | (module-map proc module): iterate obarray bindings |
| `scm_c_module_bound_p(SCM *, SCM *)` | 693 | Scheme-level | (module-bound? module symbol) |
| `scm_c_module_p(SCM *)` | 708 | Scheme-level | (module? obj) |

#### Internal Helpers

| Function | Line | Status | Description |
|----------|------|--------|-------------|
| `module_name_to_path(SCM_List *name)` | 239 | internal | Convert module name list to file path, e.g. (ice-9 common-list) -> "ice-9/common-list.scm" |
| `file_exists(const char *path)` | 287 | internal | Check if file exists on filesystem |
| `register_module_roots()` | 1177 | internal | Register module globals as GC roots |
| `init_modules()` | 1182 | init | Create root module, init registry, register Scheme built-in functions |

### Scheme-Level Module API: Guile 1.8 vs pscm

| Guile 1.8 Procedure / Special Form | pscm Equivalent | Status | Implementation |
|------------------------------------|----------------|--------|----------------|
| `define-module` | `eval_define_module` | present | `src/c/module.cc:713`, special form dispatch in `eval.cc:333` |
| `use-modules` | `eval_use_modules` | present | `src/c/module.cc:838`, dispatch `eval.cc:337` |
| `export` | `eval_export` | present | `src/c/module.cc:874`, dispatch `eval.cc:341` |
| `re-export` | `eval_re_export` | present | `src/c/module.cc:893`, dispatch `eval.cc:345` |
| `define-public` | `eval_define_public` | present | `src/c/module.cc:923`, dispatch `eval.cc:349` |
| `module?` | `scm_c_module_p` | present | `src/c/module.cc:708` |
| `current-module` | `scm_c_current_module` | present | `src/c/module.cc:499` |
| `set-current-module` | `scm_c_set_current_module` | present | `src/c/module.cc:504` |
| `resolve-module` | `scm_c_resolve_module` | present | `src/c/module.cc:554` (SCM) / `scm_c_resolve_module(const char*)` at line 566 (C string) |
| `module-ref` | `scm_c_module_ref` | present | `src/c/module.cc:635`, vararg with optional default value |
| `module-bound?` | `scm_c_module_bound_p` | present | `src/c/module.cc:693` |
| `module-use!` | `scm_c_module_use` | present | `src/c/module.cc:1035` |
| `module-map` | `scm_c_module_map` | present | `src/c/module.cc:950` |
| `set-module-binder!` | `scm_c_set_module_binder` | present | `src/c/module.cc:1135` |
| `module-export!` | -- | missing | No Scheme-level `module-export!` function; use `export` special form instead |
| `module-name` | -- | missing | `SCM_Module::name` C field exists but not exposed as Scheme getter |
| `module-public-interface` | -- | partial | No direct procedure; accessible via `(module-ref mod '%module-public-interface)` in scm_c_module_ref (line 668) |
| `module-kind` | -- | missing | `SCM_Module::kind` C field exists but not exposed as Scheme getter |
| `module-obarray` | -- | missing | Not exposed to Scheme |
| `module-binder` (getter) | -- | partial | Setter exists as `set-module-binder!` but getter `module-binder` does not |
| `resolve-interface` | -- | missing | `scm_ensure_public_interface()` is an internal C helper (line 445), not a Scheme procedure |
| `%module-public-interface` | handled in scm_c_module_ref | present | Internal symbol accessible via `(module-ref mod '%module-public-interface)` at line 668 |
| `make-module` | -- | missing | C-level `scm_make_module` exists (line 16) but not exposed as Scheme procedure |
| `save-module` | -- | missing | Not implemented (Guile 1.8 uses this for compiled module cache) |

### define-module Option Parsing

Parsed in `eval_define_module()` (`src/c/module.cc`, lines 756-830). The option parser iterates keyword-value pairs from the expression list.

| Option | Status | Implementation Lines | Description |
|--------|--------|---------------------|-------------|
| `#:use-module` (mod-name) | present | 762-775 | Resolves the module, adds its public interface to uses list |
| `#:export` (sym ...) | present | 777-786 | Calls `scm_module_export()` for each symbol |
| `#:pure` | present | 787-789 | Sets module kind to `'pure` |
| `#:autoload` (mod-name) sym ... | present | 790-822 | Stores autoload spec; lazy-loaded on first symbol access |
| `#:no-backtrace` | present | 823-826 | Sets module kind to `'no-backtrace` |
| `#:renamer` | missing | -- | Not parsed; no symbol renaming support |
| `#:duplicates` | missing | -- | Not parsed; no duplicate import handling |

Note: The parser is described as "simplified version" (comment on line 726). Options are matched by literal string comparison on keyword symbols. The `#:use-module` option resolves modules before `define-module` has finished, which may interact with `export` ordering.

### SCM_Module Struct Field Inventory

Defined in `src/c/pscm_types.h`, lines 124-135.

| Field | Type | Description | Guile 1.8 Equivalent | Notes |
|-------|------|-------------|---------------------|-------|
| `obarray` | `SCM_HashTable *` | Local bindings hash table (symbol -> value) | `obarray` | Present in both; uses `eq?` key comparison |
| `uses` | `SCM_List *` | List of used module objects | `uses` | Linked list, not a vector as in Guile |
| `binder` | `SCM_Procedure *` | Dynamic binding procedure (module sym definep) -> variable | `binder` | Same concept; recurses into module_search_variable |
| `eval_closure` | `SCM_Procedure *` | Lookup strategy function | `eval_closure` | Present in both; used for custom lookup |
| `transformer` | `SCM_Procedure *` | Syntax transformer | `transformer` | Present in both; for module-level macro transformers |
| `name` | `SCM_List *` | Module name list, e.g. (guile-user) | `name` | Same in both |
| `kind` | `SCM_Symbol *` | Module type: 'module, 'interface, 'directory | `kind` | Same concept in Guile |
| `public_interface` | `SCM_Module *` | Public interface module (exports) | `public_interface` | Same; created on-demand by scm_ensure_public_interface |
| `exports` | `SCM_List *` | List of exported symbols | `exports` | Same concept in Guile |
| `autoload_specs` | `SCM_List *` | List of (mod-name . sym-list) for autoload | (extension) | pscm extension beyond Guile 1.8's define-module #:autoload |

**Missing vs Guile 1.8:**
- `system` flag: Guile 1.8 modules can have a system flag indicating built-in modules. Not present in pscm's struct.
- `observer` / `watchers`: Not present in either pscm or Guile 1.8 (added in Guile 2.x).
- `name` in Guile 1.8 is stored as a list of symbols (same as pscm).
- No separate `module-type` enum; pscm uses `kind` symbol which is more flexible.

### Key Observations

1. **Module file loading** (`scm_resolve_module`, line 297) searches `%load-path` from the root module and falls back to the current directory. If no file is found, it emits a warning.

2. **Autoload** is implemented as a lazy resolution mechanism in `module_search_variable` (lines 142-187): when a symbol is looked up that matches an autoload spec, the module is loaded, added to uses, and the lookup retries.

3. **Binder recursion guard** (`g_in_binder_call`, line 10) prevents infinite recursion when the binder body triggers symbol lookup in the same module.

4. **Public interface** is created lazily by `scm_ensure_public_interface()` (line 445) on first export. It is a separate `SCM_Module` whose `kind` is `'interface` and whose obarray is populated from `module->exports`.

5. **TeXmacs usage** (test/module/texmacs/): The `texmacs-module` macro at line 27 of boot.scm uses `define-module` with TeXmacs-specific options (`:use`, `:inherit`, `:export`) mapped through a `texmacs-module` macro, plus calls `module-use!` to include the texmacs-user module. The `inherit-modules` macro at line 11 uses `resolve-module` + `module-ref` with `'%module-public-interface` + `module-map` to enumerate exports, then calls `use-modules` + `re-export`.

6. **Module registry** uses `equal?` comparison (since module names are lists of symbols, not atoms), while the obarray uses `eq?` (symbol identity). The registry is lazily initialized.

## C API

Source: `src/c/pscm_api.h` (declarations), `src/c/pscm_api.cc` (implementations), `src/c/pscm.h` (internal `scm_*` declarations).

All public C API functions are prefixed `pscm_*` and provide `extern "C"` linkage suitable for embedding in C programs. The `pscm_api.cc` wrappers delegate to internal `scm_*` functions. Below is the catalog grouped by the three-phase design (`docs/superpowers/specs/2026-05-01-c-api-completeness-design.md`).

### Phase A: Core Embedding API

| Function | Status | Declaration | Implementation | Notes |
|----------|--------|-------------|----------------|-------|
| `pscm_init` | present | `pscm_api.h:11` | `pscm_api.cc:19` | Calls `init_scm()` |
| `pscm_cleanup` | present | `pscm_api.h:12` | `pscm_api.cc:24` | Currently no-op, reserved for future use |
| `pscm_eval` | present | `pscm_api.h:15` | `pscm_api.cc:29` | Evaluate an AST node; wrapped in `scm_c_catch` |
| `pscm_eval_string` | present | `pscm_api.h:16` | `pscm_api.cc:48` | Parse + eval a C string; wrapped in `scm_c_catch` |
| `pscm_eval_file` | present | `pscm_api.h:17` | `pscm_api.cc:68` | Parse file + eval all expressions; returns last result |
| `pscm_parse` | present | `pscm_api.h:20` | `pscm_api.cc:90` | Parse C string to single `SCM*` AST node; wrapped in `scm_c_catch` |
| `pscm_parse_file` | present | `pscm_api.h:21` | `pscm_api.cc:105` | Parse file to `SCM_List*` of expressions |
| `pscm_c_lookup` | present | `pscm_api.h:28` | `pscm_api.cc:148` | Delegates to `scm_c_lookup`; returns variable object (Guile 1.8 compatible) |
| `pscm_variable_ref` | present | `pscm_api.h:29` | `pscm_api.cc:152` | Delegates to `scm_variable_ref`; get value from variable object |
| `pscm_make_variable` | present | `pscm_api.h:30` | `pscm_api.cc:156` | Delegates to `scm_make_variable` |
| `pscm_make_undefined_variable` | present | `pscm_api.h:31` | `pscm_api.cc:160` | Delegates to `scm_make_undefined_variable` |
| `pscm_c_define` | present | `pscm_api.h:34` | `pscm_api.cc:164` | Delegates to `scm_c_define`; creates binding in current module |
| `pscm_c_define_gsubr` | present | `pscm_api.h:37` | `pscm_api.cc:168` | Delegates to `scm_c_define_gsubr`; wraps C function as Scheme procedure |
| `pscm_call_0` through `pscm_call_3` | missing | -- | -- | `scm_call_0/1/2/3` exist at internal level (`src/c/apply.cc:16-113`, `src/c/pscm.h:757-760`) but no `pscm_*` wrappers exist |
| `pscm_get_last_error_key` | present | `pscm_api.h:57` | `pscm_api.cc:209` | Returns the key from the last error caught by `scm_c_catch` |
| `pscm_get_last_error_message` | present | `pscm_api.h:58` | `pscm_api.cc:213` | Returns the error message string |

**Additional Phase A functions (beyond the design spec):**

| Function | Status | Notes |
|----------|--------|-------|
| `pscm_ithrow` | present | `pscm_api.h:50`, `pscm_api.cc:205`; Guile 1.8 compatible throw with `noreturn` flag |
| `pscm_set_error_handler` | present | `pscm_api.h:54`, `pscm_api.cc:126`; set a C callback for error reporting |
| `pscm_get_global_env` | present | `pscm_api.h:24`, `pscm_api.cc:113`; returns `&g_env` |
| `pscm_create_env` | present | `pscm_api.h:25`, `pscm_api.cc:118`; creates child environment with optional parent |
| `pscm_set_debug_enabled` | present | `pscm_api.h:61`, `pscm_api.cc:131` |
| `pscm_set_ast_debug_enabled` | present | `pscm_api.h:62`, `pscm_api.cc:135` |
| `pscm_get_debug_enabled` | present | `pscm_api.h:63`, `pscm_api.cc:139` |
| `pscm_get_ast_debug_enabled` | present | `pscm_api.h:64`, `pscm_api.cc:143` |

### Phase B: Module C API

None of the Phase B functions are exposed as `pscm_*` wrappers. They all exist at the internal `scm_*` level in `src/c/module.cc` and are declared in `src/c/pscm.h` but have no `pscm_api.h` entry point.

| Function | Status | Internal `scm_*` Location | Notes |
|----------|--------|---------------------------|-------|
| `pscm_c_resolve_module` | missing | `src/c/module.cc:566` | `scm_c_resolve_module(const char*)` exists; no `pscm_*` wrapper in `pscm_api.h` |
| `pscm_c_module_lookup` | missing | `src/c/module.cc:575` | `scm_c_module_lookup(SCM*, const char*)` exists; no `pscm_*` wrapper |
| `pscm_c_module_define` | missing | `src/c/module.cc:590` | `scm_c_module_define(SCM*, const char*, SCM*)` exists; no `pscm_*` wrapper |
| `pscm_c_use_module` | missing | `src/c/module.cc:1148` | `scm_c_use_module(const char*)` exists; no `pscm_*` wrapper |
| `pscm_c_current_module` | missing | `src/c/module.cc:499` | `scm_c_current_module(SCM*)` exists; no `pscm_*` wrapper |
| `pscm_c_set_current_module` | missing | `src/c/module.cc:504` | `scm_c_set_current_module(SCM*)` exists; no `pscm_*` wrapper |

The design spec also lists these internal functions that are present at the `scm_*` level:

| Internal Function | Present | Location | Notes |
|-------------------|---------|----------|-------|
| `scm_c_define_module` | present | `src/c/module.cc:1109` | Resolve module, run init function with module as current |
| `scm_c_call_with_current_module` | present | `src/c/module.cc:1094` | Temporarily switch module, call func, restore |
| `scm_c_define` | present | `src/c/module.cc:511` | Define in current module (wrapped as `pscm_c_define`) |
| `scm_c_define_gsubr` | present | `src/c/module.cc:525` | Register C function (wrapped as `pscm_c_define_gsubr`) |
| `scm_module_lookup` | present | `src/c/module.cc:601` | Scheme-level module lookup by SCM symbol |
| `scm_module_define` | present | `src/c/module.cc:620` | Scheme-level module define by SCM symbol |

### Phase C: Port Control API

| Function | Status | Declaration | Implementation | Notes |
|----------|--------|-------------|----------------|-------|
| `pscm_current_input_port` | present | `pscm_api.h:43` | `pscm_api.cc:182` | Delegates to `scm_current_input_port()` |
| `pscm_current_output_port` | present | `pscm_api.h:45` | `pscm_api.cc:188` | Delegates to `scm_current_output_port()` |
| `pscm_current_error_port` | present | `pscm_api.h:41` | `pscm_api.cc:174` | Delegates to `scm_current_error_port()` |
| `pscm_set_current_input_port` | present | `pscm_api.h:44` | `pscm_api.cc:185` | Delegates to `scm_set_current_input_port()` |
| `pscm_set_current_output_port` | present | `pscm_api.h:46` | `pscm_api.cc:191` | Delegates to `scm_set_current_output_port()` |
| `pscm_set_current_error_port` | present | `pscm_api.h:42` | `pscm_api.cc:178` | Delegates to `scm_set_current_error_port()` |
| `pscm_force_output` | present | `pscm_api.h:47` | `pscm_api.cc:195` | Extra; wraps `scm_force_output()` |
| `pscm_open_input_file` | missing | -- | -- | No `pscm_*` or `scm_*` C API; only Scheme-level `open-input-file` exists |
| `pscm_open_output_file` | missing | -- | -- | Only Scheme-level `open-output-file` exists |
| `pscm_open_input_string` | missing | -- | -- | Only Scheme-level `open-input-string` exists |
| `pscm_open_output_string` | missing | -- | -- | Only Scheme-level `open-output-string` exists |

### Guile 1.8 C API Compatibility

Beyond the three-phase design, the following categories of Guile 1.8 C API functions are absent:

| Function Category | Status | Notes |
|-------------------|--------|-------|
| `pscm_is_*` (type predicates) | missing | No `pscm_is_number`, `pscm_is_string`, `pscm_is_pair`, etc. (internal `is_num`, `is_str`, `is_pair` macros exist in `pscm.h`) |
| `pscm_list_*` (list C API) | missing | No `pscm_cons`, `pscm_car`, `pscm_cdr`, `pscm_list`, etc. |
| `pscm_symbol_*` (symbol C API) | missing | No `pscm_symbol_to_string`, `pscm_string_to_symbol`, etc. |
| `pscm_string_*` (string C API) | missing | No `pscm_string_length`, `pscm_string_ref`, etc. |
| `pscm_call_*` (apply C API) | partial | `scm_call_0/1/2/3` exist at internal level but no `pscm_*` wrappers |

### Summary

| Phase | Present | Missing | Notes |
|-------|---------|---------|-------|
| Phase A (Core) | 15 | 4 | `pscm_call_0/1/2/3` missing; many extras present (debug, env, throw) |
| Phase B (Module) | 0 | 6 | All 6 functions exist as `scm_*` internals; no `pscm_*` wrappers |
| Phase C (Ports) | 7 | 4 | Open functions missing; current-port functions present |
| Guile 1.8 Compatibility | 0 | 11 | No `pscm_is_*`, `pscm_list_*`, `pscm_symbol_*`, `pscm_string_*` functions |

## Numeric Tower

Sources: `src/c/number.cc` (arithmetic operations), `src/c/predicate.cc` (type predicates), `src/c/pscm.h` (conversion helpers), `src/c/pscm_types.h` (type definitions including `SCM_RATIO`).

### Type System

The numeric type system uses three value types within the `SCM` tagged union (`pscm_types.h:17-40`):

| SCM::Type | Tag | Description | Exact? |
|-----------|-----|-------------|--------|
| `NUM` | 5 | Fixnum integer stored directly as `int64_t` in `scm->value` | exact |
| `FLOAT` | 6 | IEEE 754 double-precision float stored as `double` in `scm->value` via `ptr_to_double`/`double_to_ptr` | inexact |
| `RATIO` | 13 | Rational number as `SCM_Rational` struct (numerator/denominator) | exact |

The `SCM_Rational` struct (`pscm_types.h:71-74`):
```cpp
struct SCM_Rational {
  int64_t numerator;
  int64_t denominator; // always > 0
};
```

### Generic Arithmetic

Registered in `src/c/number.cc:init_number()` (lines 961-985).

| Operation | Scheme Name | C Function | Status | Notes |
|-----------|-------------|------------|--------|-------|
| `+` | `+` | `scm_c_add_number` | present | Binary, generic (int/float/ratio). Neutral element 0. No float promotion bug for `+`. |
| `-` | `-` | `scm_c_minus_wrapper` | present | Handles unary (negate), binary, and multi-arg forms. Delegates to `scm_c_negate_number` for unary, `scm_c_minus_number` for binary. |
| `*` | `*` | `scm_c_mul_number` | present | Binary, generic. Has explicit ratio multiplication path. Neutral element 1. |
| `/` | `/` | `scm_c_div` | present | Handles unary (reciprocal), binary, and multi-arg forms. Returns ratios for exact division. Handles float fallback correctly. |
| `abs` | `abs` | `scm_c_abs` | present | **Integer only** (asserts `is_num`). Does NOT handle floats or ratios. |
| `quotient` | `quotient` | `scm_c_quotient` | present | Integer only. Division by zero error. |
| `remainder` | `remainder` | `scm_c_remainder` | present | Integer only. Uses truncating division semantics. |
| `modulo` | `modulo` | `scm_c_modulo` | present | Integer only. Has sign adjustment for Scheme's modulo semantics (differs from C `%` when signs differ). |

### Comparison Operations

All support chained comparison for 3+ arguments (e.g., `(< a b c)` means `a < b` and `b < c`). All return `#t` with zero or one argument.

| Operation | Scheme Name | C Function | Status | Notes |
|-----------|-------------|------------|--------|-------|
| `=` | `=` | `scm_c_eq_chain` | present | Float promotion handled directly in `scm_c_eq_number` (bypasses template). Handles int/float/ratio correctly. |
| `<` | `<` | `scm_c_lt_chain` | present | Uses `BinaryOperator<LtOp<bool, int64_t>>` template. **Potential bug**: float comparison truncates to `int64_t` before comparing due to template specialization, giving incorrect results for negative non-integer floats. |
| `>` | `>` | `scm_c_gt_chain` | present | Same template issue as `<`. |
| `<=` | `<=` | `scm_c_le_chain` | present | Same template issue as `<`. |
| `>=` | `>=` | `scm_c_ge_chain` | present | Same template issue as `<`. |

**Bug note for `<`, `>`, `<=`, `>=`**: The `BinaryOperator` template's float-promotion path calls `Op::run(d_lhs, d_rhs)` with `Op` = `LtOp<bool, int64_t>`, which takes `int64_t` parameters. The double arguments are truncated to `int64_t` before comparison. For example, `(< -1.5 -1.0)` evaluates `(-1 < -1)` which is false, but the correct answer is true. The `=` operator is not affected because `scm_c_eq_number` handles float promotion directly without the template.

### Type Predicates

Registered in `src/c/predicate.cc:init_predicate()` (lines 138-144) and `src/c/number.cc:init_number()` (lines 962-966).

| Predicate | Scheme Name | Status | Implementation | Notes |
|-----------|-------------|--------|----------------|-------|
| `number?` | `number?` | present | `predicate.cc:27` | Returns true for NUM, FLOAT, RATIO |
| `complex?` | `complex?` | present | `predicate.cc:31` | Same as `number?` (no complex type yet); all numbers return true |
| `real?` | `real?` | present | `predicate.cc:37` | Same as `number?` (all numbers are real, no complex type yet) |
| `rational?` | `rational?` | present | `predicate.cc:43` | Returns true for NUM and RATIO; false for FLOAT (inexact) |
| `integer?` | `integer?` | present | `predicate.cc:49` | Returns true for NUM only (exact integers); false for FLOAT and RATIO |
| `exact?` | `exact?` | present | `predicate.cc:55` | Returns true for NUM and RATIO |
| `inexact?` | `inexact?` | present | `predicate.cc:61` | Returns true for FLOAT only |
| `zero?` | `zero?` | present | `number.cc:69` | Handles NUM, FLOAT, RATIO; converts to double for comparison |
| `positive?` | `positive?` | present | `number.cc:21` | Handles NUM and FLOAT; converts to double |
| `negative?` | `negative?` | present | `number.cc:9` | Handles NUM and FLOAT; converts to double |
| `odd?` | `odd?` | present | `number.cc:33` | Handles NUM and FLOAT with truncation to int64_t; errors on non-integer floats |
| `even?` | `even?` | present | `number.cc:51` | Same as odd? |

### Advanced Arithmetic

| Operation | Scheme Name | C Function | Status | Notes |
|-----------|-------------|------------|--------|-------|
| `sqrt` | `sqrt` | `scm_c_sqrt` | present | Returns double; handles NUM, FLOAT, RATIO via `scm_to_double`. Negative arguments error. No exact result support. |
| `expt` | `expt` | `scm_c_expt` | present | **Integer only**. Uses loop multiplication. Handles `0^0=1`. For negative exponents: returns 1 for `1^n`, -1 for `-1^n`, 0 for all other cases (cannot represent fractions). No float/ratio base support. |
| `gcd` | `gcd` | `scm_c_gcd` | present | **Integer only**. Uses Euclidean algorithm. Returns 0 for no arguments (R5RS). |
| `lcm` | `lcm` | `scm_c_lcm` | present | **Integer only**. Computes `(a / gcd(a,b)) * b` to avoid overflow. Returns 1 for no arguments (R5RS). |
| `max` | `max` | `scm_c_max` | present | Handles NUM, FLOAT, RATIO. Vararg. Returns integer if all args integer and result fits, float otherwise. |
| `min` | `min` | `scm_c_min` | present | Same as max. |

### Float Precision and Conversion

| Aspect | Details |
|--------|---------|
| Float representation | IEEE 754 double-precision via `double` stored in `void*` using `ptr_to_double`/`double_to_ptr` (`pscm.h:269-282`) |
| Integer to float promotion | Automatic; when either operand is FLOAT, both are converted to double via `needs_float_promotion()` |
| Float to integer coercion | Via `scm_to_double()` which returns `double`; explicit cast to `int64_t` used in operations (truncation toward zero) |
| `scm_from_double()` | Creates a `SCM` with `type=FLOAT` and stores the double value (`pscm.h:269`) |
| `scm_to_double()` | Returns `double` for NUM (cast from `int64_t`), FLOAT (direct), and RATIO (division of numerator by denominator) (`pscm.h:282`) |

### Ratio Support

| Aspect | Status | Details |
|--------|--------|---------|
| `SCM_RATIO` type | present | `pscm_types.h:33`, with `SCM_Rational` struct at lines 71-74 |
| Construction | present | `scm_make_ratio()` at `number.cc:597`; normalizes sign, simplifies via GCD, degrades to integer if denominator=1 |
| Ratio in `*` | present | `scm_c_mul_number` at `number.cc:296-330` has explicit ratio multiplication path |
| Ratio in `/` | present | `scm_c_div` at `number.cc:774-885` handles ratio operands and returns ratios for exact division |
| Ratio in `+` | missing | `scm_c_add_number` does NOT handle ratios; will be treated as an error (no `is_ratio` check in the non-float-path) |
| Ratio in `-` | missing | `scm_c_minus_number` does NOT handle ratios; same issue |
| Ratio in comparisons | missing | `scm_c_eq_number`, `scm_c_lt_number`, etc. do not check for `is_ratio` in `needs_float_promotion`; `=` handles it via double conversion, `<`/`>`/`<=`/`>=` fall through to integer template which will misinterpret |
| Ratio in `abs` | missing | `scm_c_abs` asserts `is_num` only |
| Ratio in `sqrt` | partial | `scm_c_sqrt` checks `is_ratio` and converts to double |
| Ratio in `expt` | missing | `scm_c_expt` asserts `is_num` only |
| Ratio in predicates | present | `zero?`, `max`, `min` handle ratios via double conversion |

**Bug note for `+`, `-`, comparisons with ratios**: `scm_c_add_number` and `scm_c_minus_number` call `needs_float_promotion(lhs, rhs)` which only checks `is_float(lhs) || is_float(rhs)`. If both operands are ratios, neither is float, so the code falls into the integer path and calls `is_num` which will fail (ratio has type RATIO, not NUM). Similarly, the comparison operators `<`, `>`, `<=`, `>=` will fail when given ratio operands.

### R5RS Coverage Gaps

| R5RS Required | Status | Notes |
|---------------|--------|-------|
| `make-rectangular` | missing | No complex number support |
| `make-polar` | missing | No complex number support |
| `real-part` | missing | No complex number support |
| `imag-part` | missing | No complex number support |
| `magnitude` | missing | No complex number support (though could work on real numbers) |
| `angle` | missing | No complex number support |
| `numerator` | missing | Not implemented |
| `denominator` | missing | Not implemented |
| `rationalize` | missing | Not implemented |
| `exp` | missing | No transcendental functions |
| `log` | missing | No transcendental functions |
| `sin` | missing | No trigonometric functions |
| `cos` | missing | No trigonometric functions |
| `tan` | missing | No trigonometric functions |
| `asin` | missing | No trigonometric functions |
| `acos` | missing | No trigonometric functions |
| `atan` | missing | No trigonometric functions (single-arg or two-arg) |
| Exactness propagation | partial | Operations return exact results when both operands are exact (integer or ratio). Float operand forces inexact result. |
| Exact->inexact | missing | No `exact->inexact` procedure |
| Inexact->exact | missing | No `inexact->exact` procedure |
| `string->number` | present | Handles integer and float parsing |
| `number->string` | present | Returns decimal representation |

### Summary

| Category | Present | Partial | Missing | Total |
|----------|---------|---------|---------|-------|
| Generic Arithmetic | 8 | 0 | 0 | 8 |
| Comparison | 5 | 0 | 0 | 5 |
| Type Predicates | 11 | 0 | 0 | 11 |
| Advanced Arithmetic | 6 | 0 | 0 | 6 |
| Ratio Support (+/-/comparison) | 0 | 0 | 4 | 4 |
| Transcendental | 0 | 0 | 10 | 10 |
| Complex | 0 | 0 | 6 | 6 |
| Conversion/Exactness | 0 | 1 | 3 | 4 |

**Overall: 30 present, 1 partial, 23 missing, 54 total in R5RS numeric tower reference.**
